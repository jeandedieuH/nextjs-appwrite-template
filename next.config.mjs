/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "appwrite-j8w4oww.jdhagena.com",
      },
    ],
  },
};

export default nextConfig;
